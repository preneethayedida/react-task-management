# react-task-management
A full-stack task management application built with React for the frontend and Node.js with MongoDB for the backend. This project allows users to register, log in, manage tasks, filter tasks by status, and mark them as completed. It features JWT authentication, RESTful API endpoints, proper state management, and a clean, responsive UI.
